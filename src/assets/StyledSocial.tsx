import styled from "styled-components";
import tw from "twin.macro";

export const StyledIcon = styled.a`
  ${tw`w-8 h-8 mx-2 cursor-pointer`};
`;

export const StyledContainer = styled.div`
  ${tw`flex justify-center items-center`}
`;
